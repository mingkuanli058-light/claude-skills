现在是一名 Python工程师。

我要开发一个小工具，用来 根据项目文档自动生成网络拓扑结构图（HTML+SVG）。

请为我设计并实现一个 Python程序。

一、输入文件

程序接收三个 Markdown 文件：

建设方案.md
investment_schema.md
chart_registry.md

大致作用：

建设方案.md

描述系统结构，例如：

接入节点

应用服务器

达梦数据库

NAS存储

访问终端

用于推导系统组件关系。

investment_schema.md

描述真实设备，例如：

应用服务器 ×1
网络存储设备 ×2
达梦数据库

程序应尽量保证：

拓扑图中出现的设备 来源于该文件。

chart_registry.md

定义图件类型，例如：

A-01 系统总体架构图
A-02 网络拓扑图

程序只需要实现：

A-02 网络拓扑图
二、目标

程序读取三个文件后，生成：

network_topology.html

内容包含：

SVG拓扑图

图例

简单说明

三、拓扑结构示例

拓扑图大致结构可以类似：

访问终端
    ↓
接入节点
    ↓
应用服务器
    ↓
数据库
    ↓
NAS存储

也可以根据解析结果进行合理布局。

布局方式不做强制要求，你可以设计合理的自动布局。

四、技术要求

建议使用：

Python
SVG
HTML

可以使用：

argparse
re
jinja2

不需要使用复杂框架。

五、工程结构

请生成一个 清晰的Python工程，例如：

topology_generator
│
├─ main.py
├─ parser.py
├─ topology_builder.py
├─ html_renderer.py
│
├─ templates
│   └─ topology_template.html
│
└─ output
六、运行方式

程序运行方式：

python main.py \
  --solution 建设方案.md \
  --investment investment_schema.md \
  --registry chart_registry.md

生成：

output/network_topology.html
七、代码要求

代码要求：

Python 3

结构清晰

模块划分合理

带必要注释

八、输出要求

请输出：

1️⃣ 完整项目目录结构
2️⃣ 每个文件完整代码

如果代码较多，请 按文件逐个输出，不要截断。

这一版提示词的优点

Claude Code 会：

自动设计 解析逻辑

自动设计 拓扑布局

自动写 SVG绘制

自动组织 工程结构

而不是只写死代码。

这就是 “可发挥型提示词”。